apiVersion: v1
kind: Service
metadata:
  name: {{ include "kannan-aks-image.fullname" . }}
  labels:
    {{- include "kannan-aks-image.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kannan-aks-image.selectorLabels" . | nindent 4 }}
