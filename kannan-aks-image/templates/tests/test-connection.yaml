apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kannan-aks-image.fullname" . }}-test-connection"
  labels:
    {{- include "kannan-aks-image.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kannan-aks-image.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
